# Student Information Server

This is a Node.js + Express server that provides student information.

## Routes

- GET / → Welcome message
- GET /about → Student details
- GET /contact → Contact info
- POST /register → Register student
- PUT /update → Update student

## How to run

1. Install dependencies:
   npm install

2. Run server:
   npm run dev

Server runs on http://localhost:3000
